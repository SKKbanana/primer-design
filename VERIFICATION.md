# 验证记录

日期：2026-09-24。程序版本：1.0.0。

## 指令与坐标验证

使用真实 `primer3_core`（libprimer3 2.6.1，来自 primer3-py 2.3.1 的配套可执行程序）以 `--strict_tags` 读取生成的输入文件。测试中的 DNA 为固定随机种子生成的人工序列，不采用用户实验数据。

10 项核心检查通过，涵盖 FASTA 整理、单条记录限制、非法字符、1-based 闭区间到 0-based 起点/长度转换、单碱基目标、重复匹配、反向互补定位、缺失/越界参数、产物范围、克隆附加序列和巢式内引物范围。

9 项真实引擎检查通过，均无 `PRIMER_ERROR`，并独立核对了返回引物的坐标、模板结合序列、反向互补方向和产物长度：

| 场景 | 返回引物对数 | 第 1 对的产物长度 |
| --- | ---: | ---: |
| 普通 PCR 包围目标 | 5 | 839 bp |
| qPCR 区域内设计 | 5 | 138 bp |
| 克隆精确边界 | 5 | 750 bp |
| 克隆含两侧附加序列 | 5 | 762 bp（含两侧共 12 nt 附加序列） |
| 巢式外引物 | 5 | 659 bp |
| 巢式内引物 | 5 | 659 bp |
| 精确扩增整个模板 | 2 | 1200 bp |
| 包围单碱基目标 | 5 | 839 bp |
| 反向互补目标定位 | 5 | 839 bp |

外引物和内引物测试为独立配置用例；相同的首选产物长度不代表一套已联合选定的巢式方案。内引物用例使用人工指定的外引物结合区作为边界。

另检查过一组含酶切位点的附加序列：Primer3 能读取输入，但因候选的发卡结构等条件返回 0 对。这是有效的筛选结果，说明“文件生成成功”不保证一定存在合格引物。最终测试的附加序列用例选用能返回候选的人工尾序列，以便独立核验方向与长度。

## 浏览器检查

在 Linux 上通过 Chromium 153 以本地 `file://` 方式运行：

- 本地打开、演示载入与指令生成正常。
- 下载内容逐字节等于预览文本，文件无 BOM。
- 修改输入立即停用旧输出与下载按钮。
- qPCR、巢式内引物和克隆尾序列的界面交互生效。
- 目标序列定位、多处匹配选择、非法输入阻止导出正常。
- FASTA 文件导入正常。
- 1440 px 桌面和 390 px 手机宽度下布局无水平溢出。
- 刷新后输入清空；交互期间没有外部 HTTP 请求，也没有 JavaScript 页面错误。

浏览器共覆盖 13 类检查。已检查桌面和手机宽度的页面截图。

## 验证范围

这是软件输入生成与位置约束验证，不是湿实验有效性验证。未在 Windows、macOS、Firefox、Safari 上分别执行测试；程序采用普通浏览器 API，具体环境仍可能有差异。Windows/Git Bash 等命令依据 Primer3 的命令行接口编写，但未在这些操作系统上实际运行。

未核验特定基因的生物学身份、全基因组特异性、扩增效率或实验成功率。交付包不包含 Primer3 可执行程序、其数据库或参数文件。
